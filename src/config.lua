local config = {
  enabled = true;
  HephLegendaryBonus = false;
}

local configDesc = {
  enabled = "Whether the mod is enabled or not.";
  HephLegendaryBonus = "Whether Hephaestus' Legendary boon is buffed (0.5 seconds instead of 1.0 seconds).";
}

return config, configDesc
